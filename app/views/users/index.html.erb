<section id="users">
  <h1>All Users</h1>
  <% @users.each do |user| %>
    <%= render partial: "shared/userbar", locals: {
      photo: user.photo,
      username: user.name,
      post_number: user.posts_counter,
      id: user.id
    } 
    %>
  <% end %>
</section>
